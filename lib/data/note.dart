class Note {
  //  how to save object as a tring

  // Assing model class to string
  // String value = jsonEncode(ModelCountry.countries[185])

  // Assing String to Model
  // ModelCountry modelCountry = ModelCountry.fromJson(jsonDecode(dataSetting.modelCountry));

  // flutter pub get && flutter pub run build_runner build --delete-conflicting-outputs
}
